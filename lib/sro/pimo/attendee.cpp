/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "attendee.h"

#include "socialevent.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::Attendee::Attendee()
  : PersonRole( QUrl(), QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#Attendee") )
{
}



Nepomuk2::Attendee::Attendee( const Attendee& res )
  : PersonRole( res )
{
}


Nepomuk2::Attendee::Attendee( const Nepomuk2::Resource& res )
  : PersonRole( res )
{
}


Nepomuk2::Attendee::Attendee( const QString& uri )
  : PersonRole( uri, QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#Attendee") )
{
}

Nepomuk2::Attendee::Attendee( const QUrl& uri )
  : PersonRole( uri, QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#Attendee") )
{
}

Nepomuk2::Attendee::Attendee( const QString& uri, const QUrl& type )
  : PersonRole( uri, type )
{
}


Nepomuk2::Attendee::Attendee( const QUrl& uri, const QUrl& type )
  : PersonRole( uri, type )
{
}

Nepomuk2::Attendee::~Attendee()
{
}

Nepomuk2::Attendee& Nepomuk2::Attendee::operator=( const Attendee& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::Attendee::resourceTypeUri()
{
    return QLatin1String("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#Attendee");
}

QList<Nepomuk2::SocialEvent> Nepomuk2::Attendee::attendingMeetings() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<SocialEvent> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#attendingMeeting") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( SocialEvent( *it ) );
    return rl;
}

void Nepomuk2::Attendee::setAttendingMeetings( const QList<Nepomuk2::SocialEvent>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<SocialEvent>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#attendingMeeting"), Variant( l ) );
}

void Nepomuk2::Attendee::addAttendingMeeting( const Nepomuk2::SocialEvent& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#attendingMeeting") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#attendingMeeting"), v );
}

QUrl Nepomuk2::Attendee::attendingMeetingUri()
{
    return QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/11/01/pimo#attendingMeeting");
}


