/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "resource.h"

#include "image.h"
#include "filedataobject.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::Resource::Resource()
  : Resource( QUrl(), QUrl::fromEncoded("http://www.w3.org/2000/01/rdf-schema#Resource") )
{
}



Nepomuk2::Resource::Resource( const Resource& res )
  : Resource( res )
{
}


Nepomuk2::Resource::Resource( const Nepomuk2::Resource& res )
  : Resource( res )
{
}


Nepomuk2::Resource::Resource( const QString& uri )
  : Resource( uri, QUrl::fromEncoded("http://www.w3.org/2000/01/rdf-schema#Resource") )
{
}

Nepomuk2::Resource::Resource( const QUrl& uri )
  : Resource( uri, QUrl::fromEncoded("http://www.w3.org/2000/01/rdf-schema#Resource") )
{
}

Nepomuk2::Resource::Resource( const QString& uri, const QUrl& type )
  : Resource( uri, type )
{
}


Nepomuk2::Resource::Resource( const QUrl& uri, const QUrl& type )
  : Resource( uri, type )
{
}

Nepomuk2::Resource::~Resource()
{
}

Nepomuk2::Resource& Nepomuk2::Resource::operator=( const Resource& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::Resource::resourceTypeUri()
{
    return QLatin1String("http://www.w3.org/2000/01/rdf-schema#Resource");
}

QList<Nepomuk2::Image> Nepomuk2::Resource::depictions() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Image> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/03/22/nfo#depiction") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( Image( *it ) );
    return rl;
}

void Nepomuk2::Resource::setDepictions( const QList<Nepomuk2::Image>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<Image>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/03/22/nfo#depiction"), Variant( l ) );
}

void Nepomuk2::Resource::addDepiction( const Nepomuk2::Image& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/03/22/nfo#depiction") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/03/22/nfo#depiction"), v );
}

QUrl Nepomuk2::Resource::depictionUri()
{
    return QUrl::fromEncoded("http://www.semanticdesktop.org/ontologies/2007/03/22/nfo#depiction");
}


