/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "series.h"

#include "publication.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::Series::Series()
  : InformationElement( QUrl(), QUrl::fromEncoded("http://www.example.com/nbib#Series") )
{
}



Nepomuk2::Series::Series( const Series& res )
  : InformationElement( res )
{
}


Nepomuk2::Series::Series( const Nepomuk2::Resource& res )
  : InformationElement( res )
{
}


Nepomuk2::Series::Series( const QString& uri )
  : InformationElement( uri, QUrl::fromEncoded("http://www.example.com/nbib#Series") )
{
}

Nepomuk2::Series::Series( const QUrl& uri )
  : InformationElement( uri, QUrl::fromEncoded("http://www.example.com/nbib#Series") )
{
}

Nepomuk2::Series::Series( const QString& uri, const QUrl& type )
  : InformationElement( uri, type )
{
}


Nepomuk2::Series::Series( const QUrl& uri, const QUrl& type )
  : InformationElement( uri, type )
{
}

Nepomuk2::Series::~Series()
{
}

Nepomuk2::Series& Nepomuk2::Series::operator=( const Series& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::Series::resourceTypeUri()
{
    return QLatin1String("http://www.example.com/nbib#Series");
}

QStringList Nepomuk2::Series::issns() const
{
    return ( property( QUrl::fromEncoded("http://www.example.com/nbib#issn") ).toStringList());
}

void Nepomuk2::Series::setIssns( const QStringList& value )
{
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#issn"), Variant( value ) );
}

void Nepomuk2::Series::addIssn( const QString& value )
{
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#issn") );
    v.append( value );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#issn"), v );
}

QString Nepomuk2::Series::issn() const
{
    return ( property( QUrl::fromEncoded("http://www.example.com/nbib#issn") ).toStringList() << QString() ).first();
}

void Nepomuk2::Series::setIssn( const QString& value )
{
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#issn"), Variant( value ) );
}

QUrl Nepomuk2::Series::issnUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#issn");
}

QList<Nepomuk2::Publication> Nepomuk2::Series::seriesOfs() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Publication> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.example.com/nbib#seriesOf") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( Publication( *it ) );
    return rl;
}

void Nepomuk2::Series::setSeriesOfs( const QList<Nepomuk2::Publication>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<Publication>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#seriesOf"), Variant( l ) );
}

void Nepomuk2::Series::addSeriesOf( const Nepomuk2::Publication& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#seriesOf") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#seriesOf"), v );
}

QUrl Nepomuk2::Series::seriesOfUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#seriesOf");
}


