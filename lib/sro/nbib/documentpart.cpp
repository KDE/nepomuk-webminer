/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "documentpart.h"

#include "reference.h"
#include "publication.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::DocumentPart::DocumentPart()
  : InformationElement( QUrl(), QUrl::fromEncoded("http://www.example.com/nbib#DocumentPart") )
{
}



Nepomuk2::DocumentPart::DocumentPart( const DocumentPart& res )
  : InformationElement( res )
{
}


Nepomuk2::DocumentPart::DocumentPart( const Nepomuk2::Resource& res )
  : InformationElement( res )
{
}


Nepomuk2::DocumentPart::DocumentPart( const QString& uri )
  : InformationElement( uri, QUrl::fromEncoded("http://www.example.com/nbib#DocumentPart") )
{
}

Nepomuk2::DocumentPart::DocumentPart( const QUrl& uri )
  : InformationElement( uri, QUrl::fromEncoded("http://www.example.com/nbib#DocumentPart") )
{
}

Nepomuk2::DocumentPart::DocumentPart( const QString& uri, const QUrl& type )
  : InformationElement( uri, type )
{
}


Nepomuk2::DocumentPart::DocumentPart( const QUrl& uri, const QUrl& type )
  : InformationElement( uri, type )
{
}

Nepomuk2::DocumentPart::~DocumentPart()
{
}

Nepomuk2::DocumentPart& Nepomuk2::DocumentPart::operator=( const DocumentPart& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::DocumentPart::resourceTypeUri()
{
    return QLatin1String("http://www.example.com/nbib#DocumentPart");
}

QList<Nepomuk2::Publication> Nepomuk2::DocumentPart::documentPartOfs() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Publication> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( Publication( *it ) );
    return rl;
}

void Nepomuk2::DocumentPart::setDocumentPartOfs( const QList<Nepomuk2::Publication>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<Publication>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf"), Variant( l ) );
}

void Nepomuk2::DocumentPart::addDocumentPartOf( const Nepomuk2::Publication& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf"), v );
}

Nepomuk2::Publication Nepomuk2::DocumentPart::documentPartOf() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    return Publication( property( QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf") ).toResource().uri() );
}

void Nepomuk2::DocumentPart::setDocumentPartOf( const Nepomuk2::Publication& value )
{
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf"), Variant( value ) );
}

QUrl Nepomuk2::DocumentPart::documentPartOfUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#documentPartOf");
}


