/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "legislation.h"

#include "codeoflaw.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::Legislation::Legislation()
  : LegalDocument( QUrl(), QUrl::fromEncoded("http://www.example.com/nbib#Legislation") )
{
}



Nepomuk2::Legislation::Legislation( const Legislation& res )
  : LegalDocument( res )
{
}


Nepomuk2::Legislation::Legislation( const Nepomuk2::Resource& res )
  : LegalDocument( res )
{
}


Nepomuk2::Legislation::Legislation( const QString& uri )
  : LegalDocument( uri, QUrl::fromEncoded("http://www.example.com/nbib#Legislation") )
{
}

Nepomuk2::Legislation::Legislation( const QUrl& uri )
  : LegalDocument( uri, QUrl::fromEncoded("http://www.example.com/nbib#Legislation") )
{
}

Nepomuk2::Legislation::Legislation( const QString& uri, const QUrl& type )
  : LegalDocument( uri, type )
{
}


Nepomuk2::Legislation::Legislation( const QUrl& uri, const QUrl& type )
  : LegalDocument( uri, type )
{
}

Nepomuk2::Legislation::~Legislation()
{
}

Nepomuk2::Legislation& Nepomuk2::Legislation::operator=( const Legislation& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::Legislation::resourceTypeUri()
{
    return QLatin1String("http://www.example.com/nbib#Legislation");
}

QList<Nepomuk2::Resource> Nepomuk2::Legislation::coSponsors() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.example.com/nbib#coSponsor") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( Resource( *it ) );
    return rl;
}

void Nepomuk2::Legislation::setCoSponsors( const QList<Nepomuk2::Resource>& value )
{
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#coSponsor"), Variant( value ) );
}

void Nepomuk2::Legislation::addCoSponsor( const Nepomuk2::Resource& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#coSponsor") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#coSponsor"), v );
}

QUrl Nepomuk2::Legislation::coSponsorUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#coSponsor");
}

QList<Nepomuk2::CodeOfLaw> Nepomuk2::Legislation::codeOfLaws() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<CodeOfLaw> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( CodeOfLaw( *it ) );
    return rl;
}

void Nepomuk2::Legislation::setCodeOfLaws( const QList<Nepomuk2::CodeOfLaw>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<CodeOfLaw>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw"), Variant( l ) );
}

void Nepomuk2::Legislation::addCodeOfLaw( const Nepomuk2::CodeOfLaw& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw"), v );
}

Nepomuk2::CodeOfLaw Nepomuk2::Legislation::codeOfLaw() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    return CodeOfLaw( property( QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw") ).toResource().uri() );
}

void Nepomuk2::Legislation::setCodeOfLaw( const Nepomuk2::CodeOfLaw& value )
{
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw"), Variant( value ) );
}

QUrl Nepomuk2::Legislation::codeOfLawUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#codeOfLaw");
}


