/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "courtreporter.h"

#include "legalcasedocument.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::CourtReporter::CourtReporter()
  : Collection( QUrl(), QUrl::fromEncoded("http://www.example.com/nbib#CourtReporter") )
{
}



Nepomuk2::CourtReporter::CourtReporter( const CourtReporter& res )
  : Collection( res )
{
}


Nepomuk2::CourtReporter::CourtReporter( const Nepomuk2::Resource& res )
  : Collection( res )
{
}


Nepomuk2::CourtReporter::CourtReporter( const QString& uri )
  : Collection( uri, QUrl::fromEncoded("http://www.example.com/nbib#CourtReporter") )
{
}

Nepomuk2::CourtReporter::CourtReporter( const QUrl& uri )
  : Collection( uri, QUrl::fromEncoded("http://www.example.com/nbib#CourtReporter") )
{
}

Nepomuk2::CourtReporter::CourtReporter( const QString& uri, const QUrl& type )
  : Collection( uri, type )
{
}


Nepomuk2::CourtReporter::CourtReporter( const QUrl& uri, const QUrl& type )
  : Collection( uri, type )
{
}

Nepomuk2::CourtReporter::~CourtReporter()
{
}

Nepomuk2::CourtReporter& Nepomuk2::CourtReporter::operator=( const CourtReporter& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::CourtReporter::resourceTypeUri()
{
    return QLatin1String("http://www.example.com/nbib#CourtReporter");
}

QList<Nepomuk2::LegalCaseDocument> Nepomuk2::CourtReporter::legalCases() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<LegalCaseDocument> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.example.com/nbib#legalCase") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( LegalCaseDocument( *it ) );
    return rl;
}

void Nepomuk2::CourtReporter::setLegalCases( const QList<Nepomuk2::LegalCaseDocument>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<LegalCaseDocument>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#legalCase"), Variant( l ) );
}

void Nepomuk2::CourtReporter::addLegalCase( const Nepomuk2::LegalCaseDocument& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#legalCase") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#legalCase"), v );
}

QUrl Nepomuk2::CourtReporter::legalCaseUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#legalCase");
}


