/*
 * This file is part of the Nepomuk KDE project.
 *
 * This library is free software; you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation; either version 2 of the License, or
 * (at your option) any later version.
 * See the file "COPYING" for the exact licensing terms.
 */

/*
 * This file has been generated by the Nepomuk Resource class generator.
 * DO NOT EDIT THIS FILE.
 * ANY CHANGES WILL BE LOST.
 */

#include <nepomuk2/variant.h>
#include <nepomuk2/resourcemanager.h>
#include "article.h"

#include "collection.h"

#include <QtCore/QDateTime>
#include <QtCore/QDate>
#include <QtCore/QTime>


Nepomuk2::Article::Article()
  : Publication( QUrl(), QUrl::fromEncoded("http://www.example.com/nbib#Article") )
{
}



Nepomuk2::Article::Article( const Article& res )
  : Publication( res )
{
}


Nepomuk2::Article::Article( const Nepomuk2::Resource& res )
  : Publication( res )
{
}


Nepomuk2::Article::Article( const QString& uri )
  : Publication( uri, QUrl::fromEncoded("http://www.example.com/nbib#Article") )
{
}

Nepomuk2::Article::Article( const QUrl& uri )
  : Publication( uri, QUrl::fromEncoded("http://www.example.com/nbib#Article") )
{
}

Nepomuk2::Article::Article( const QString& uri, const QUrl& type )
  : Publication( uri, type )
{
}


Nepomuk2::Article::Article( const QUrl& uri, const QUrl& type )
  : Publication( uri, type )
{
}

Nepomuk2::Article::~Article()
{
}

Nepomuk2::Article& Nepomuk2::Article::operator=( const Article& res )
{
    Resource::operator=( res );
    return *this;
}

QString Nepomuk2::Article::resourceTypeUri()
{
    return QLatin1String("http://www.example.com/nbib#Article");
}

QList<Nepomuk2::Collection> Nepomuk2::Article::collections() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Collection> rl;
    QList<Resource> l = property( QUrl::fromEncoded("http://www.example.com/nbib#collection") ).toResourceList() ;
    for( QList<Resource>::const_iterator it = l.constBegin();
        it != l.constEnd(); ++it )
    rl.append( Collection( *it ) );
    return rl;
}

void Nepomuk2::Article::setCollections( const QList<Nepomuk2::Collection>& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    QList<Resource> l;
    for( QList<Collection>::const_iterator it = value.constBegin();
         it != value.constEnd(); ++it ) {
        l.append( Resource( (*it) ) );
    }
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#collection"), Variant( l ) );
}

void Nepomuk2::Article::addCollection( const Nepomuk2::Collection& value )
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    Variant v = property( QUrl::fromEncoded("http://www.example.com/nbib#collection") );
    v.append( Resource( value ) );
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#collection"), v );
}

Nepomuk2::Collection Nepomuk2::Article::collection() const
{
    // We always store all Resource types as plain Resource objects.
    // It does not introduce any overhead (due to the implicit sharing of
    // the data and has the advantage that we can mix setProperty calls
    // with the special Resource subclass methods.
    // More importantly Resource loads the data as Resource objects anyway.
    return Collection( property( QUrl::fromEncoded("http://www.example.com/nbib#collection") ).toResource().uri() );
}

void Nepomuk2::Article::setCollection( const Nepomuk2::Collection& value )
{
    setProperty( QUrl::fromEncoded("http://www.example.com/nbib#collection"), Variant( value ) );
}

QUrl Nepomuk2::Article::collectionUri()
{
    return QUrl::fromEncoded("http://www.example.com/nbib#collection");
}


