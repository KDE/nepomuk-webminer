

 * WebMinerIndexingQue -> force que to add all files/fodler specified
 * service/scheduler add dbus method to fill queue manually
 
 * automatic indexer use service if available
 * automatic indexer add switch to call it directly

 
 * if we use "force" remove old data first?